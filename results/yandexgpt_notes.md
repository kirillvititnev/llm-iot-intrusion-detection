# YandexGPT

## Первый запуск

Код, сгенерированный YandexGPT, не запустился сразу.

## Ошибка (1)

```text
FileNotFoundError: [Errno 2] No such file or directory: 'data/raw/IoT_Intrusion_Detection_Dataset.csv'


## уточнение промпта
Я запустил сгенерированный тобой код, но получил ошибку:

FileNotFoundError: [Errno 2] No such file or directory: 'data/raw/IoT_Intrusion_Detection_Dataset.csv'

В папке data/raw/ находится файл IoT_Intrusion.csv.

Исправь код так, чтобы он не использовал жестко заданное имя CSV-файла, а автоматически находил первый CSV-файл в папке data/raw/. Остальную логику ML-пайплайна не меняй: загрузка данных, предобработка, обучение моделей, расчет метрик и сохранение результатов должны остаться.

## Второй запуск

Код опять не запустился. Создаем пробуем следующий промпт для исправления ошибок и запуска:

Я повторно запустил исправленный код. Ошибка с поиском CSV-файла исправлена, но теперь программа падает на этапе сохранения лучшей модели.

## Ошибка (2)
## Уточнение промпта
KeyError: 'accuracy'

Traceback указывает на строку:

best_model = max(models, key=lambda x: evaluate_models(X_test, y_test, {x: models[x]})['accuracy'])

evaluate_models возвращает результаты не в виде словаря с прямым ключом 'accuracy', поэтому выбор лучшей модели реализован некорректно.



Исправь код так, чтобы:
1. Метрики для всех моделей считались один раз.
2. Результаты сохранялись в словарь или DataFrame.
3. Лучшая модель выбиралась по macro F1-score, а не по accuracy.
4. Лучшая обученная модель сохранялась в models/best_model.pkl.
5. Для LogisticRegression увеличь max_iter до 1000.
6. В precision_score, recall_score, f1_score и classification_report добавь zero_division=0.
7. Остальную логику программы не меняй.


## Третий запуск

После второго уточняющего промпта код загрузил датасет, выполнил первичный анализ и дошел до этапа обучения моделей. Ошибка с поиском CSV-файла была исправлена.

Однако программа снова завершилась ошибкой:

```text
KeyError: 'f1'


## Четвертый запуск
## Финальный запуск после уточняющих промптов

После точечного промпта для исправления функции `save_best_model()` код YandexGPT был успешно запущен до конца.

Программа выполнила:
- загрузку датасета `IoT_Intrusion.csv`;
- автоматическое определение целевого столбца `label`;
- первичный анализ данных;
- предобработку признаков;
- обучение моделей Random Forest, Logistic Regression и Decision Tree;
- расчет метрик;
- сохранение результатов;
- выбор лучшей модели по macro F1-score.

### Предупреждение

Во время обучения Logistic Regression появилось предупреждение:

```text
ConvergenceWarning: lbfgs failed to converge after 1000 iteration(s)

Это предупреждение не остановило выполнение программы. Оно означает, что Logistic Regression не успела сойтись за заданное число итераций. Для такого большого многоклассового датасета это ожидаемая проблема.
## Итоговый результат
Лучшая модель YandexGPT-версии:
Decision Tree
Macro F1-score:
0.8398930070134959
## Вывод
YandexGPT смогла сгенерировать рабочий ML-пайплайн, но только после нескольких уточняющих промптов. Основные проблемы были связаны не с общей структурой программы, а с деталями реализации: неправильным именем CSV-файла, ошибкой в логике выбора лучшей модели и несогласованностью структуры словаря метрик.